// Named re-export barrel — the form that already worked before the fix
// (`findExportByName` is applied on this path). Kept as a control so a
// regression here is visible.
export { namedHelper } from './named-impl';
